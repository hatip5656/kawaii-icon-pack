# Temporary diagnostic: if this is cancelled too, the problem is account-level
# (Actions billing / permissions), not the other workflow files. Delete after.
name: Hello

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "runner reached"
